# Design

> Visual and interaction principles for instances with a UI. `/init-project` prunes this file if the chosen stack has no UI.

## Design tokens

_Where tokens live (colors, spacing, type scale) and the rule that components consume tokens, never raw values._

## Component system

_The component library/approach, where components live, and the customization rule (wrap, don't fork)._

## Interaction principles

_Loading, error, and empty states are mandatory for every data surface. Navigation and URL-state conventions._

## Voice

_Microcopy tone, capitalization, terminology — must agree with `CONTEXT.md` vocabulary._
