# Generated Docs

Machine-owned directory. Every file here (except this README) is produced by a generator command and overwritten on regeneration — **never hand-edit**.

When the stack is wired, generators land here: e.g. `make generate-docs` producing `db-schema.md` from the ORM models. Each generated file must start with a comment naming the command that produces it.
