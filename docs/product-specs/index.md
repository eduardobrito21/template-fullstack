# Product Specs

One-line index of product specs (PRDs). Add an entry when adding a spec. Specs capture the *what* and *why* of a feature; the *how* lives in an exec plan that cites the spec.

_No specs yet._
